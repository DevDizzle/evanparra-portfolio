---
import BaseLayout from '../../layouts/BaseLayout.astro';
import SectionHeading from '../../components/SectionHeading.astro';

const title = "Local Business Automation Audits | Evan Parra";
const description = "Find the $50K hiding in your manual processes. Automation audits for local businesses in St. Augustine and Jacksonville.";
---

<BaseLayout title={title} description={description}>
  <!-- Hero -->
  <section class="bg-gradient-to-br from-slate-900 via-slate-800 to-emerald-900 text-white py-24 md:py-32 relative overflow-hidden">
    <div class="absolute inset-0 bg-[url('/grid.svg')] bg-center [mask-image:linear-gradient(180deg,white,rgba(255,255,255,0))]"></div>
    <div class="container relative z-10">
      <div class="max-w-4xl mx-auto text-center">
        <h1 class="text-4xl md:text-6xl font-bold leading-tight mb-6">Local Business Automation Audits</h1>
        <p class="text-xl md:text-2xl text-slate-300">Find the $50K hiding in your manual processes.</p>
      </div>
    </div>
  </section>

  <!-- The Problem -->
  <section class="py-20 bg-white">
    <div class="container">
      <SectionHeading title="The Problem" align="center" />
      <div class="max-w-3xl mx-auto text-lg text-slate-600 space-y-4 text-center">
        <p>Local businesses in St. Augustine and Jacksonville are drowning in manual work:</p>
        <ul class="text-left inline-block space-y-2 mt-4">
            <li>• Answering the same phone calls over and over</li>
            <li>• Manually entering data from forms and invoices</li>
            <li>• Chasing down appointments and follow-ups</li>
            <li>• Copying info between systems that don't talk to each other</li>
        </ul>
        <p class="mt-4 font-semibold">Every hour spent on repetitive tasks is an hour not spent growing the business.</p>
      </div>
    </div>
  </section>

  <!-- The Solution -->
  <section class="py-20 bg-slate-50">
    <div class="container">
      <SectionHeading title="The Solution" align="center" />
      <div class="max-w-3xl mx-auto mt-8">
        <p class="text-lg text-slate-600 mb-8 text-center">I audit your operations and identify automation opportunities:</p>
        <ul class="grid gap-4 md:grid-cols-2">
          {[
            "Phone/text automation (AI answering, appointment booking)",
            "Document processing (invoices, forms, contracts)",
            "CRM and scheduling integration",
            "Custom workflows that connect your tools"
          ].map(item => (
            <li class="flex items-start bg-white p-4 rounded-lg shadow-sm">
              <span class="text-emerald-600 mr-3">✓</span>
              {item}
            </li>
          ))}
        </ul>
      </div>
    </div>
  </section>

  <!-- Who This Is For -->
  <section class="py-20 bg-white">
      <div class="container">
        <SectionHeading title="Who This Is For" align="center" />
        <div class="flex flex-wrap justify-center gap-4 max-w-4xl mx-auto mt-8">
            {[
                "Contractors (HVAC, plumbing, electrical, roofing)",
                "Medical/dental practices",
                "Law firms and accountants",
                "Property management companies",
                "Any local business doing $500K+ revenue with manual operations"
            ].map(item => (
                <span class="px-5 py-3 bg-emerald-50 text-emerald-900 font-medium rounded-lg border border-emerald-100">
                    {item}
                </span>
            ))}
        </div>
      </div>
  </section>

  <!-- Pricing -->
  <section class="py-20 bg-slate-900 text-white">
    <div class="container">
        <div class="max-w-4xl mx-auto grid md:grid-cols-2 gap-8">
            <div class="bg-slate-800 p-8 rounded-2xl border border-slate-700">
                <h3 class="text-2xl font-bold mb-4">Audit Only</h3>
                <p class="text-4xl font-bold text-emerald-400 mb-4">$500</p>
                <p class="text-slate-400">Credited toward implementation if you decide to build with me.</p>
            </div>
            <div class="bg-slate-800 p-8 rounded-2xl border border-slate-700">
                <h3 class="text-2xl font-bold mb-4">Audit + Implementation</h3>
                <p class="text-4xl font-bold text-emerald-400 mb-4">Custom</p>
                <p class="text-slate-400">Quote provided based on the scope and complexity of the automation.</p>
            </div>
        </div>
    </div>
  </section>

  <!-- How It Works -->
  <section class="py-20 bg-slate-50">
    <div class="container">
      <SectionHeading title="How It Works" align="center" />
      <div class="max-w-4xl mx-auto mt-12 grid gap-8 md:grid-cols-4">
        {[
          { title: "Free Discovery Call", desc: "30 minutes to understand your business" },
          { title: "On-Site or Remote Audit", desc: "Map your current processes" },
          { title: "Automation Report", desc: "Prioritized list of opportunities with ROI estimates" },
          { title: "Implementation (Optional)", desc: "Build the automations if you want to move forward" }
        ].map((step, i) => (
          <div class="relative">
            <div class="text-6xl font-bold text-slate-200 absolute -top-4 -left-4 -z-10">{i + 1}</div>
            <h3 class="text-xl font-bold text-slate-900 mb-2">{step.title}</h3>
            <p class="text-slate-600">{step.desc}</p>
          </div>
        ))}
      </div>
      <div class="text-center mt-12">
          <p class="text-slate-500 italic">Service Area: St. Augustine, Jacksonville, Palm Coast, and surrounding Northeast Florida. Remote audits available nationwide.</p>
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="py-24 bg-emerald-600 text-white text-center">
    <div class="container">
      <h2 class="text-3xl md:text-4xl font-bold mb-6">Stop doing robot work.</h2>
      <a href="https://cal.com/evanparra" class="inline-block bg-white text-emerald-600 px-8 py-4 rounded-xl font-bold text-lg hover:bg-slate-100 transition-colors shadow-lg">
        Book Your Free Discovery Call
      </a>
    </div>
  </section>
</BaseLayout>
